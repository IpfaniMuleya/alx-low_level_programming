#include "main.h"
/**
 * mul - gets the product of two numbers
 * @a: first variable
 * @b: second variable
 * Return: product of two integers
 */
int mul(int a, int b)
{
	int result = a * b;

	return (result);
}
