#include <stdio.h>

/**
 * main - the main function is the starting point for program execution
 *
 * Return: gives 0 when there are no errors
 */

int main(void)
{
	printf("with proper grammar, but the outcome is a piece of art,\n");
	return (0);
}
